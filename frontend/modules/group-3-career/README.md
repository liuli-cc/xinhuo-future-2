# Group 3 · 岗位匹配与投递复盘

负责岗位原文导入、要求提取、证据匹配、岗位补强任务、投递阶段和复盘。

- `client/`：岗位解析与匹配纯函数。
- `components/`：岗位工作台专属 React 组件。
- `backend/app/modules/`：FastAPI/MySQL 的基础模块已接入；岗位解析、匹配与复盘业务接口仍待迁移。
- 路由入口保留在 `app/career/`。

本模块可以读取二组产出的已核验能力证据，但不得绕过共享鉴权直接访问其他学生数据。未迁移接口会显式失败，不会回退到旧数据源。
